---
// HeroSectionのテキストはダミーを使用
const mainText = "「夜空を見上げるたび、星の言葉が降り注ぐ。」";
const subText = "それは、失われた叡智の書(アカシャ)であり、未来を拓く預言の詩(オラトリオ)";
---
<section class="relative w-full min-h-[700px] flex items-center justify-center overflow-hidden bg-black">
  <!-- シンプルなグラデーション背景 -->
  <div class="absolute inset-0 w-full h-full bg-gradient-to-b from-black via-gray-900 to-black z-0"></div>
  <!-- コンテンツ -->
  <div class="relative z-20 flex flex-col items-center justify-center w-full px-4 pt-48 pb-32">
    <h1 class="text-2xl md:text-3xl lg:text-4xl font-light text-white text-center mb-6 animate-fadein-glow will-change-transform will-change-opacity" style="text-shadow:0 4px 24px #000,0 1px 0 #fff;">
      {mainText}
    </h1>
    <p class="text-xl md:text-2xl text-white text-center mb-8 animate-fadein-delay font-light will-change-transform will-change-opacity">
      {subText}
    </p>
    <div class="flex flex-col sm:flex-row gap-6 justify-center mb-0 mt-8">
      <a href="#" class="px-5 py-3 rounded-full border-2 border-white text-white font-bold flex items-center gap-2 bg-transparent hover:text-primary transition-all duration-200 text-base tracking-wide shadow-lg hover:shadow-2xl hover:scale-105 animate-fadein-delay2">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        LISTEN ON STREAMING
      </a>
      <a href="#" class="px-5 py-3 rounded-full border-2 border-white text-white font-bold flex items-center gap-2 bg-transparent hover:text-primary transition-all duration-200 text-base tracking-wide shadow-lg hover:shadow-2xl hover:scale-105 animate-fadein-delay2">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M8 12l2 2 4-4"/></svg>
        WATCH MUSIC VIDEO
      </a>
    </div>
  </div>
  <style>
    @keyframes fadein-glow {
      0% { opacity: 0; transform: translateY(40px) scale(0.98); }
      100% { opacity: 1; transform: translateY(0) scale(1); }
    }
    .animate-fadein-glow {
      animation: fadein-glow 1.2s cubic-bezier(.4,0,.2,1) both;
    }
    .animate-fadein-delay {
      animation: fadein-glow 1.2s cubic-bezier(.4,0,.2,1) both;
      animation-delay: 0.5s;
    }
    .animate-fadein-delay2 {
      animation: fadein-glow 1.2s cubic-bezier(.4,0,.2,1) both;
      animation-delay: 1s;
    }
  </style>
</section>
